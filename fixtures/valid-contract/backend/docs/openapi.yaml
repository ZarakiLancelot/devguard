openapi: 3.0.3
info:
  title: Digital Library API
  version: 1.0.0
paths: {}
components:
  schemas:
    UpdateBookRequest:
      type: object
      required:
        - isbn
        - active
      properties:
        isbn:
          type: string
        active:
          type: boolean
        tags:
          type: array
          items:
            type: string
