openapi: 3.0.3
info:
  title: Customer Store API
  version: 1.0.0
paths: {}
components:
  schemas:
    UpdateCustomerStoreRequest:
      type: object
      required:
        - compositeRank
      properties:
        compositeRank:
          type: integer
        storeNumber:
          type: string
