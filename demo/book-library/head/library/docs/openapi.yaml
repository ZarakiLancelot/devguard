openapi: 3.0.3
info:
  title: Book Library API
  version: 1.1.0
paths: {}
components:
  schemas:
    UpdateBookRequest:
      type: object
      required:
        - isbn
        - pageCount
        - authorId
      properties:
        isbn:
          type: string
        pageCount:
          type: integer
        authorId:
          type: integer
