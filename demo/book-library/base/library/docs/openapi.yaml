openapi: 3.0.3
info:
  title: Book Library API
  version: 1.0.0
paths: {}
components:
  schemas:
    UpdateBookRequest:
      type: object
      required:
        - isbn
        - pageCount
      properties:
        isbn:
          type: string
        pageCount:
          type: integer
